name: lint proyect

on:
	push: 
		branches: [ main ]

jobs: 
	build: 
		runs-on: ubuntu-lastest 
		
		steps: 
			- uses: actions/checkout@v4
			- uses: denoland/setup-deno@v2
			  with:
			    deno-version: vx.x.x
		  - run: deno fmt --check
